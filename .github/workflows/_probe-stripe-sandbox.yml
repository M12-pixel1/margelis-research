name: Probe Stripe sandbox CLI

on:
  push:
    branches:
      - benchmark/v0.2-p0-stripe-sandbox

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Install Stripe CLI
        run: npm install -g @stripe/cli@latest
      - name: Show Stripe CLI syntax only
        run: |
          stripe --version
          stripe sandbox create --help
          stripe payment_intents create --help
          stripe payment_intents retrieve --help
