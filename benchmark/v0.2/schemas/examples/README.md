# Synthetic schema examples

The files in this directory are **synthetic examples** that show the shape of a
receipt and are validated against `../receipt.schema.json` in CI.

They are **not benchmark results**. They describe no real system, run, person or
organization. All identifiers use the `EXAMPLE` prefix or the reserved
`example.test` domain, and all digests are placeholders.
